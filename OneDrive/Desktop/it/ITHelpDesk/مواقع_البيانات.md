# مواقع حفظ البيانات - نظام IT Help Desk

## 📊 قاعدة البيانات (Database)

### الموقع الحالي:
- **النوع**: SQL Server LocalDB
- **الخادم**: `(localdb)\mssqllocaldb`
- **اسم القاعدة**: `ITHelpDesk`

### الملف الفعلي على الجهاز:
```
C:\Users\{اسم_المستخدم}\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\mssqllocaldb\
```

### كيفية الوصول:
1. افتح **SQL Server Management Studio (SSMS)**
2. اتصل بـ: `(localdb)\mssqllocaldb`
3. ستجد قاعدة البيانات `ITHelpDesk`

---

## 📁 الصور والملفات المرفقة

### الموقع الحالي:
```
C:\Users\USER\OneDrive\Desktop\it\ITHelpDesk\wwwroot\uploads\{رقم_التذكرة}\
```

### مثال:
- تذكرة رقم 1: `wwwroot\uploads\1\`
- تذكرة رقم 8: `wwwroot\uploads\8\` ✅ (موجود حالياً)

### هيكل المجلدات:
```
wwwroot/
└── uploads/
    ├── 1/          (ملفات تذكرة رقم 1)
    │   └── abc123.jpg
    ├── 8/          (ملفات تذكرة رقم 8) ✅
    │   └── 39b543f4de944...
    └── ...
```

---

## 🔧 تغيير المواقع للإنتاج

### قاعدة البيانات - SQL Server كامل:
عدّل `appsettings.Production.json`:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=اسم_السيرفر;Database=ITHelpDesk;User Id=اسم_المستخدم;Password=كلمة_المرور;TrustServerCertificate=True;"
  }
}
```

### الملفات - مجلد مخصص:
عدّل `TicketAttachmentService.cs`:
```csharp
// بدلاً من wwwroot
var uploadsFolder = Path.Combine(@"D:\ITHelpDeskFiles", "uploads", ticketId.ToString());
```

---

## 📋 معلومات مهمة

### قاعدة البيانات تحتوي على:
- ✅ المستخدمين (Users)
- ✅ التذاكر (Tickets)
- ✅ المرفقات (TicketAttachments) - **فقط معلومات الملفات**
- ✅ سجلات النشاط (TicketLogs)

### الملفات الفعلية (الصور/PDF):
- ✅ تُحفظ في `wwwroot/uploads/`
- ✅ **لا تُحفظ في قاعدة البيانات** (فقط المسار)

---

## 🔍 التحقق من المواقع

### قاعدة البيانات:
```powershell
sqlcmd -S "(localdb)\mssqllocaldb" -Q "SELECT name FROM sys.databases"
```

### الملفات:
```powershell
Get-ChildItem -Path "wwwroot\uploads" -Recurse
```

---

**ملاحظة**: في الإنتاج، استخدم SQL Server كامل وليس LocalDB!

